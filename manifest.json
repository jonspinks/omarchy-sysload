{
  "schemaVersion": 1,
  "id": "blacksheep.sysload",
  "name": "System Load",
  "version": "1.0.0",
  "author": "Jon",
  "license": "MIT",
  "description": "One dial for how hard the machine is working, and the numbers behind it on hover or click",
  "kinds": ["bar-widget"],
  "entryPoints": { "barWidget": "Panel.qml" },
  "barWidget": {
    "displayName": "System Load",
    "description": "A single strain gauge driven by whichever of CPU, memory, disk I/O or temperature has run out first, with per-resource meters, rates and top processes in the panel.",
    "category": "System",
    "aliases": ["sysload", "system-load", "strain"],
    "allowMultiple": false,
    "defaultSection": "right",
    "defaults": {
      "style": "arc",
      "interval": 2,
      "idleInterval": 4
    },
    "schema": [
      {
        "key": "style",
        "type": "string",
        "label": "Gauge style",
        "description": "arc for a 270-degree dial, bar for a flat fill"
      },
      {
        "key": "interval",
        "type": "number",
        "label": "Sample interval (open)",
        "description": "Seconds between samples while the panel is open; the panel also walks the process table at this rate"
      },
      {
        "key": "idleInterval",
        "type": "number",
        "label": "Sample interval (closed)",
        "description": "Seconds between samples while only the bar gauge is visible"
      }
    ]
  }
}
